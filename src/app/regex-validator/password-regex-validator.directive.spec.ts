import { PasswordRegexValidatorDirective } from './password-regex-validator.directive';

describe('PasswordRegexValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordRegexValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
