import { MobileRegexValidatorDirective } from './mobile-regex-validator.directive';

describe('MobileRegexValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileRegexValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
