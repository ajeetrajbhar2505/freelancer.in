import { EmailRegexValidatorDirective } from './email-regex-validator.directive';

describe('EmailRegexValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailRegexValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
