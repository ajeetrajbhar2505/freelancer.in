import { RestrictMultiSpaceRegexValidatorDirective } from './restrict-multi-space-regex-validator.directive';

describe('RestrictMultiSpaceRegexValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictMultiSpaceRegexValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
