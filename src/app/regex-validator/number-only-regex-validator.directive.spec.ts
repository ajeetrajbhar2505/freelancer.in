import { NumberOnlyRegexValidatorDirective } from './number-only-regex-validator.directive';

describe('NumberOnlyRegexValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberOnlyRegexValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
